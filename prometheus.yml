global:
  scrape_interval: 5s
scrape_configs:
  - job_name: "load_balancer"
    static_configs:
      - targets: ["https://load-balancer.up.railway.app"] # Docker service name + port
        labels:
          service: "loadbalancer"
